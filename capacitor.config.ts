import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ion.bootstrap',
  appName: 'ion-bootstrap',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
